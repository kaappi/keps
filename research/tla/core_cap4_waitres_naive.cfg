\* core scenario with the NAIVE Finding-2 repair (EOF waits for reserved = 0
\* but the §4 failure path still rings only send_waiters). EXPECTED TO FAIL:
\* TLC finds a deadlock — a receiver parked through the reservation window is
\* never woken when the last reserved send's copy fails — see README.md,
\* Finding 3.
SPECIFICATION Spec
CONSTANTS
  Cap = 4
  SweepPolicy = "flip_all"
  FailCopy = TRUE
  Scenario = "core"
  EofPolicy = "wait_reserved_naive"
  RecvFail = FALSE
  RvRing = "ring"
  RvPopWithdraw = "at_pop"
  RvAbandon = "none"
INVARIANTS
  TypeOK
  RcAccounting
  DestroyedClean
  EnvelopeAccounting
  ReservedAccounting
  ParkedImpliesHeld
  CycleLeakOnly
