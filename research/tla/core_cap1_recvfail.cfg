\* core scenario, capacity 1, amended protocol, RECEIVE-side copy failures
\* only: the re-queue-at-head rule under backpressure, including the
\* transient capacity overshoot (a failed receive re-queues after its pop
\* already reopened the slot to a sender). Liveness checked. Expected to
\* PASS.
SPECIFICATION Spec
CONSTANTS
  Cap = 1
  SweepPolicy = "flip_all"
  FailCopy = FALSE
  Scenario = "core"
  EofPolicy = "wait_reserved"
  RecvFail = TRUE
  RvRing = "ring"
INVARIANTS
  TypeOK
  RcAccounting
  DestroyedClean
  EnvelopeAccounting
  ReservedAccounting
  ParkedImpliesHeld
  CycleLeakOnly
PROPERTIES
  Termination
