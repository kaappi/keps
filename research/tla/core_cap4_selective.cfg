\* core scenario with the sweep EXACTLY as §5 specifies it (flip only
\* fibers whose channel is ready-for-them). EXPECTED TO FAIL: TLC finds
\* the lost-wakeup deadlock — see README.md, Finding 1.
SPECIFICATION Spec
CONSTANTS
  Cap = 4
  SweepPolicy = "selective"
  FailCopy = FALSE
  Scenario = "core"
  EofPolicy = "asis"
INVARIANTS
  TypeOK
  RcAccounting
  DestroyedClean
  EnvelopeAccounting
  ReservedAccounting
  ParkedImpliesHeld
  CycleLeakOnly
