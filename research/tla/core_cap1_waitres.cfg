\* core scenario, capacity 1, amended protocol, send-side copy failures,
\* liveness checked. Expected to PASS. (Receive-side failures live in
\* core_cap1_recvfail — combining both failure dimensions with liveness at
\* this bound is beyond a reasonable TLC budget; core_cap4_waitres covers
\* the combination for safety.)
SPECIFICATION Spec
CONSTANTS
  Cap = 1
  SweepPolicy = "flip_all"
  FailCopy = TRUE
  Scenario = "core"
  EofPolicy = "wait_reserved"
  RecvFail = FALSE
  RvRing = "ring"
INVARIANTS
  TypeOK
  RcAccounting
  DestroyedClean
  EnvelopeAccounting
  ReservedAccounting
  ParkedImpliesHeld
  CycleLeakOnly
PROPERTIES
  Termination
